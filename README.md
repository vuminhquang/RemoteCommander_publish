# RemoteCommander (npm packages)

Connect your computer to ChatGPT, Claude or any MCP client:

```
npx remote-commander@latest remote
```

This repository holds the published npm packages only: the launcher and the prebuilt binaries for each OS. Its workflows check that nothing else lands here, run each binary on its own OS, and publish to npm with provenance.
